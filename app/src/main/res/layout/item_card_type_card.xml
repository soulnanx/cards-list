<?xml version="1.0" encoding="utf-8"?>
<layout>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content">

    <androidx.cardview.widget.CardView
        android:id="@+id/cardTypeCardView"
        android:layout_width="@dimen/card_type_card_view_width"
        android:layout_height="@dimen/card_type_card_view_height"
        android:layout_margin="@dimen/margin_2x"
        app:cardCornerRadius="@dimen/card_type_corner"
        app:cardElevation="@dimen/card_type_elevation"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent">

        <androidx.constraintlayout.widget.ConstraintLayout
            android:id="@+id/cardTypeView"
            android:layout_width="match_parent"
            android:layout_height="match_parent">

            <TextView
                android:id="@+id/cardTypeTitleTextView"
                style="@style/defaultTV"
                android:layout_marginStart="@dimen/margin_4x"
                android:layout_marginBottom="@dimen/margin_3x"
                android:textColor="@android:color/white"
                android:textSize="@dimen/text_size_2x"
                app:layout_constraintBottom_toBottomOf="@id/cardTypeView"
                app:layout_constraintStart_toStartOf="@id/cardTypeView"
                tools:text="Druid" />
        </androidx.constraintlayout.widget.ConstraintLayout>
    </androidx.cardview.widget.CardView>

</androidx.constraintlayout.widget.ConstraintLayout>
</layout>